package com.example.catchcompass.user;

/**
 * How measurements are shown to this angler. Storage is always metric; this
 * only affects display, per the README.
 */
public enum UnitPreference {
    METRIC,
    US_CUSTOMARY
}
