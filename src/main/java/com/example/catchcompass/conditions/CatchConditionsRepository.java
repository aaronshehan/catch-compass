package com.example.catchcompass.conditions;

import org.springframework.data.jpa.repository.JpaRepository;

public interface CatchConditionsRepository extends JpaRepository<CatchConditions, Long> {
}
